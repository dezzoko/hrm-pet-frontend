import { StyledVariables } from '../types/styled.types';

export const defaultSizes:StyledVariables = {
    sizes: {
        header: {
            height: 56,
        },
        borderRadius: {
            small: 4,
            medium: 8,
            large: 12,
            round: 50,
        },

    },
};
